# Intentionally vulnerable lab

These manifests are negative fixtures for scanners and admission tests. They are isolated from `kubernetes/secure` and must never be deployed to a shared or production cluster. Every weakness is intentional and mapped to a testable control.
