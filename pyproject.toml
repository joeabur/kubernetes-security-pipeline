[project]
name = "kubernetes-security-pipeline"
version = "0.1.0"
description = "DevSecOps demonstration pipeline for containerized Kubernetes workloads"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]
